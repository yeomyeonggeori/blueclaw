{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://blueclaw.dev/schema/soul.schema.json",
  "title": "soul",
  "description": "How the agent carries itself: what it holds to, what it never does, how it works, and how it speaks. One document per workspace, written by the host and read by the runtime before every turn.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion"],
  "properties": {
    "schemaVersion": { "type": "integer", "const": 1 },
    "values": { "type": "array", "maxItems": 8, "uniqueItems": true, "items": { "type": "string", "minLength": 1, "maxLength": 200 }, "description": "What the agent holds to, one sentence each." },
    "boundaries": { "type": "array", "maxItems": 8, "uniqueItems": true, "items": { "type": "string", "minLength": 1, "maxLength": 200 }, "description": "What the agent never does, one sentence each." },
    "workingStyle": { "type": "array", "maxItems": 8, "uniqueItems": true, "items": { "type": "string", "minLength": 1, "maxLength": 200 }, "description": "How the agent approaches work, one sentence each." },
    "tone": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "register": { "type": "string", "enum": ["formal", "polite", "casual"], "description": "The default level of formality." },
        "traits": { "type": "array", "maxItems": 6, "uniqueItems": true, "items": { "type": "string", "minLength": 1, "maxLength": 40 }, "description": "Adjectives for how the agent sounds." }
      }
    },
    "language": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "default": { "type": "string", "pattern": "^[a-z]{2,3}(-[A-Za-z0-9]{2,8})*$", "description": "The language the agent writes in when nothing else decides it, as a BCP 47 tag." },
        "matchRequester": { "type": "boolean", "description": "Whether the agent answers in the language the requester wrote in." }
      }
    }
  }
}
