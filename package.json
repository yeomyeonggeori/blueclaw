{
	"name": "blueclaw",
	"private": true,
	"scripts": {
		"test": "bun run typecheck && cd protocol && bun run test && cd ../chatd && bun run test && cd ../admin && bun run test",
		"typecheck": "cd protocol && bun run typecheck && cd ../chatd && bun run typecheck"
	},
	"workspaces": [
		"protocol",
		"chatd",
		"admin"
	]
}
